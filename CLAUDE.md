# CLAUDE.md

이 문서는 Claude가 School Work Toolbox 저장소에서 작업할 때 이해해야 할 프로젝트 원칙을 정리합니다.

## 기본 원칙

School Work Toolbox는 교사의 반복적인 학교 업무를 줄이기 위한 웹 기반 도구 모음입니다. 각 기능은 독립 모듈로 개발하고, 나중에 하나의 GitHub Pages 웹사이트로 통합합니다.

이 저장소는 Codex, Claude, 사용자가 번갈아 작업할 수 있습니다. 따라서 수정 전 반드시 현재 파일 상태를 확인하고, 다른 AI나 사용자가 이미 작업한 내용을 덮어쓰지 않도록 주의해야 합니다.

## 작업 시작 전 확인할 문서

- `README.md`: 프로젝트 목적, 구조, 개발 방향
- `PROJECT_STATUS.md`: 현재 완료/개발 중/예정 상태
- `TODO.md`: 앞으로 구현할 작업 목록

작업을 시작하기 전 위 문서와 Git 변경사항을 확인합니다.

## 개발 구조

- 각 기능은 `modules/` 아래 독립 폴더에서 개발합니다.
- 공통 CSS, JavaScript, 이미지 등은 `shared/` 아래에 둡니다.
- 최종 웹사이트 통합은 `website/` 폴더에서 진행합니다.
- GitHub Pages에서 실행 가능한 정적 웹 프로젝트 구조를 유지합니다.
- 사용자가 명시적으로 요청하지 않는 한 npm, 프레임워크, 서버 구조를 추가하지 않습니다.

## 구현 원칙

- 순수 HTML/CSS/JavaScript 기반 구현을 우선합니다.
- 계산 로직과 UI 로직은 가능한 한 분리합니다.
- 서버가 필요하지 않은 기능은 브라우저 로컬 처리로 구현합니다.
- 변경 범위를 최소화합니다.
- 기존 기능을 임의로 삭제하거나 대규모로 변경하지 않습니다.
- 필요한 설명만 코드에 작성하고 과도한 주석은 피합니다.

## 개인정보 보호 원칙

- 학생 및 교직원 개인정보를 외부 서버로 전송하지 않습니다.
- 실제 학생/교직원 개인정보가 포함된 샘플 데이터를 만들거나 사용하지 않습니다.
- 테스트 데이터는 가상 데이터만 사용합니다.
- 개인정보 처리가 필요한 경우 가능한 한 브라우저 내부 처리와 사용자의 직접 파일 저장 방식을 우선합니다.

## 작업 완료 후 확인

- 작업 완료 후 `PROJECT_STATUS.md`와 `TODO.md`를 갱신합니다.
- 중요한 설계 변경이 있으면 `README.md`에도 반영합니다.
- 기능을 추가한 경우 브라우저에서 직접 실행 가능한 구조인지 확인합니다.
- 불필요한 외부 라이브러리, 빌드 도구, 임시 파일을 추가하지 않았는지 확인합니다.
