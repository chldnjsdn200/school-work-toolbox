# AGENTS.md

이 문서는 Codex가 School Work Toolbox 저장소에서 작업할 때 따라야 할 개발 원칙을 정리합니다.

## 작업 전 확인

- 작업 시작 전 `README.md`, `PROJECT_STATUS.md`, `TODO.md`를 확인합니다.
- 현재 파일 상태와 Git 변경사항을 먼저 확인합니다.
- 기존 파일이나 기능을 임의로 삭제하지 않습니다.

## 개발 구조

- 각 기능은 `modules/` 아래 독립 폴더에서 개발합니다.
- 여러 기능에서 함께 쓰는 CSS, JavaScript, 자산은 `shared/` 아래에 둡니다.
- 나중에 `website/` 폴더에서 여러 모듈을 하나의 GitHub Pages 웹사이트로 통합할 수 있도록 구조를 유지합니다.
- GitHub Pages에서 실행 가능한 정적 웹 프로젝트 구조를 유지합니다.
- npm 프로젝트, 프레임워크, 서버 구조는 사용자가 명시적으로 요청하기 전까지 도입하지 않습니다.

## 코드 원칙

- 순수 HTML/CSS/JavaScript 기반 구현을 우선합니다.
- 계산 로직과 UI 로직은 가능한 한 분리합니다.
- 서버가 꼭 필요하지 않은 기능은 브라우저에서 처리합니다.
- 변경 범위를 최소화합니다.
- 기존 기능을 임의로 삭제하거나 대규모 변경하지 않습니다.
- 코드에는 필요한 설명만 작성하고 과도한 주석은 피합니다.

## 개인정보 및 테스트 데이터

- 학생 및 교직원 개인정보를 외부 서버로 전송하지 않습니다.
- 실제 학생/교직원 개인정보 샘플을 사용하지 않습니다.
- 테스트 데이터는 반드시 가상 데이터를 사용합니다.
- 개인정보를 저장해야 하는 기능은 가능한 한 브라우저 로컬 처리와 사용자의 명시적 파일 저장 방식을 우선 검토합니다.

## 작업 완료 전 확인

- 작업 완료 후 `PROJECT_STATUS.md`와 `TODO.md`를 갱신합니다.
- 중요한 설계 변경이 있으면 `README.md`에도 반영합니다.
- 기능을 추가한 경우 브라우저에서 직접 실행 가능한지 확인합니다.
- 불필요한 외부 라이브러리, 빌드 도구, 임시 파일이 추가되지 않았는지 확인합니다.
